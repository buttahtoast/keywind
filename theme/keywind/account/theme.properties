# Real Keycloak Account Console = React SPA (keycloak.v3).
# styles/scripts restyle that SPA to match Keywind (login palette + landscape).
#
# FreeMarker pages in this directory are local UI mocks only
# (npm run mock:generate + npm run dev → /html/account/).
parent=keycloak.v3
deprecatedMode=false

styles=public/keywind-account.css
scripts=public/keywind-account.js

developmentMode=false
